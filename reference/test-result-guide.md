## 작성원칙
- 'template\NEW_ERP_FM_SRM26080752974_테스트 결과서_ZSBFMBR0580_20260810.xlsx' 템플릿을 참고하여 작성.
- 파일명은 'NEW_ERP_{모듈명}_{SR NO}_테스트 결과서_{프로그램ID}_{영업일(토 · 일 · 공휴일 제외) 기준 오늘로부터 3일 후}.xlsx'으로 할 것
- 수정 세부 사항에 명시한 셀만 수정하도록 함.
- {프로그램ID}·{프로그램명}·{모듈명}·{현재유저}의 정의와 도출 규칙은 AGENTS.md `## 프로그램 식별 정보` 참고
- 입력자료
  - SR 상세 : SR 분석가가 정리한 SR 요약본을 우선 활용, 없는 경우 ITSM SR 상세를 직접 조회하여 작성
  - 비교 분석 결과 : ABAP 소스분석가가 zdown 수정 전 · 후 소스를 비교 분석한 결과(차이점 · 비즈니스적 의미) — 단위통합테스트결과서 · 사용자 테스트의 시나리오 작성 근거로 사용
- ABAP 소스분석가의 비교 분석 결과가 없으면 단위통합테스트결과서 · 사용자 테스트 항목을 작성할 수 없으므로 작성 진행 불가
- 날짜는 yyyy.mm.dd 형식으로 작성할 것.
- 수정해야 하는 셀의 범위 내에 있던 기존 값은 삭제 후 작성할 것.
- **template 잔존값 주의**: 템플릿은 이전 SR(SRM26080752974)의 값이 들어있음. 수정 대상 셀은 **빈 칸이 아니어도 반드시 이번 SR 값으로 덮어쓸 것.** 특히 시나리오 제목(C4:E4, C3:D3)·업무영역(C3)은 template 값이 남아있어도 그대로 두면 안 됨. 작성 후 **수정 대상 셀에 template의 이전 SR 문구가 남아있지 않은지 대조 확인**
- **병합 셀(Merged Cell) 처리**: C4:E4·C3:D3 등 병합 셀은 병합이 쓰기를 막는 것이 아님. **병합 영역의 좌상단 셀(C4, C3)에만 `Range("C4").Value2 = ...`로 쓰면 됨.** "셀이 병합돼서 못 썼다"는 성립하지 않음 — 병합 여부와 무관하게 좌상단 셀에 기입할 것

### 필수사항
- 항상 원본 문서의 복사본을 만들어 수정할 것.
- 모든 문서는 한글로 작성.

### 금지사항
- 원본 문서는 수정 금지.
- 위의 수정사항 이외의 계산식 및 열너비, 셀병합 등의 레이아웃 절대 변경 금지  

## 편집 방법(.xlsx 파일)
- **Python은 이 환경에서 사용 불가** — `python`은 Microsoft Store 스텁(exit 9009)이고 `pip` · `py` · `conda`가 없어 openpyxl 등 어떤 라이브러리도 설치·실행 불가. Windows PowerShell 5.1 + Excel COM(`New-Object -ComObject Excel.Application`)만 사용할 것
- Open 전 `DisplayAlerts = $false` · `Visible = $true` 설정 — 저장 시 뜨는 대화상자가 COM 자동화에서 응답받지 못해 무한 대기하는 것을 방지
- 셀 수정은 `Range("C3").Value2 = ...` 형태로 수행 (계산식 셀은 절대 건드리지 않음)
- 저장 시 `SaveAs`에 파일 경로와 포맷을 지정 (`xlOpenXMLWorkbook` = 51)
- 편집이 실패하더라도 Excel 프로세스가 잔류하지 않도록 try/finally 구조로 워크북 `Close`, 애플리케이션 `Quit` + `[GC]::Collect()`를 반드시 호출할 것 — 미종료 시 백그라운드 프로세스가 파일을 잠금
- `.ps1` 스크립트는 ASCII 문자만 사용하고 한글 텍스트(파일명 · 내용)는 UTF-8 JSON 파일로 분리할 것
- 대괄호 `[ ]`가 들어간 파일명은 `Copy-Item`/`Move-Item`에 `-LiteralPath`를 사용하고, .NET API는 `[IO.File]` 메서드를 사용할 것
- 산출물 4종 병렬 생성 시 Excel COM 스크립트(FP · 테스트결과서) 동시 실행 금지 — 실행 전 `Get-Process EXCEL` 잔류 0 확인 후 단독 실행

## 수정 세부 사항

### 0. SR 정보 및 동료검토 정보
- C3(반영예정일) : 돌아오는 목요일 (오늘이 목요일인 경우 오늘이 아니라 다음 주 목요일을 의미함)
- C4(SR번호) : {SR NO}
- C5(요청자) : ITSM에서 서비스운영현황 > 변경/데이터관련요청 > SR 목록에서 {SR_NO}를 검색해 나온 '등록자'
- C6(요구사항) : ITSM에서 서비스운영현황 > 변경/데이터관련요청 > SR 목록에서 {SR_NO}를 검색해 나온 '제목'
- C7(요구사항 변경) : 수정하지 않음.
- C8(연관모듈) : {모듈명}
- C9(T-CODE) : {프로그램ID}
- F4:F7, F10, F11 : {현재유저}
- F13 : ITSM에서 서비스운영현황 > 변경/데이터관련요청 > SR 목록에서 {SR_NO}를 검색해 나온 '등록자'
- G4:G9 : {현재일자}
- G10:G13 : 영업일(토 · 일 · 공휴일 제외) 기준 오늘로부터 3일 후
- H4:H13 : 수정하지 않음

### 1. 영향도 검토서
- ITSM SR 요청내용을 분석하여 B8:D10의 범위에 모듈/유형/대상프로그램명을 작성
  - 수정 대상 프로그램이 여러 건인 경우 빈칸부터 모두 작성
  - B열(모듈) : {모듈명}
  - C열(유형) : REPORT 또는 FUNCTION 중 작성
  - D열(대상) : {프로그램ID}
  - 입력예시 : FM/REPORT/ZSBFMBR0580
- E38:E40 : 영업일(토 · 일 · 공휴일 제외) 기준 오늘로부터 3일 후   ***누락되지 않도록 주의 — E38만 쓰고 E39·E40을 빠뜨리는 실수가 반복됨. 세 셀 모두에 동일 일자 기입 후, 생성된 파일에서 E38·E39·E40 세 칸이 모두 채워졌는지 반드시 재확인***

### 4. 단위통합테스트결과서
- ABAP 소스분석가의 비교 분석 결과를 근거로 QA테스터가 테스트 시나리오를 작성
- 개발테스트결과 · 품질테스트결과는 실제로 SAP GUI에서 테스트를 수행한 결과가 아니라, 초안 단계에서
  채워두는 예상값임 (실제 테스트는 사람이 나중에 수행하여 필요 시 값을 수정함) — 정직한 보고 규칙
  위반이 아님
- C3:E4(업무영역) : 함수가 걸려있으므로 수정하지 않음.
- C4:E4(테스트 시나리오) : 테스트 시나리오의 제목 - 단위시험을 포괄하는 내용의 시나리오 제목을 작성. **병합 셀이므로 좌상단 C4에 기입. template의 이전 SR 제목이 남아있어도 반드시 이번 SR 제목으로 덮어쓸 것** (누락 빈발)
- C6:C11(단위시험 시나리오) : 비교 분석 결과에서 식별된 변경사항 기준으로 시나리오를 순차적으로 작성.
  칸 수(6개)만큼 채우거나, 식별된 시나리오가 그보다 적으면 있는 만큼만 채움 (빈 행을 억지로 채우지 않음)
- E6:E11(예상결과) : 시나리오에 대한 예상 결과를 작성. **간결하게 작성할 것** — 검증 포인트 한 문장(조건 → 기대 결과)으로 충분하며, 주문·정산·취소 금액과 수량을 모두 나열하는 긴 서술 금지. 예: `정산완료 건 취소 시 차감 후 0원 → 취소전표 미발행` (X: `주문금액 100,000원·주문수량 1건, 정산금액 100,000원·정산수량 1건이면 차감 후 금액과 수량이 0원·0건임. 취소전표 미발행...`)
- F6:F11(Path 및 T-Code) : {프로그램ID}
- G6:G11(입력값) : 아래 고정 마지막 행을 제외한 모든 시나리오는 '하기처리내용참고' 입력
- H6:H11(개발테스트결과) : 아래 고정 마지막 행을 제외한 모든 시나리오는 '정상' 입력
- I6:I11(품질테스트결과) : 아래 고정 마지막 행을 제외한 모든 시나리오는 '정상' 입력
- 단위시험 시나리오의 가장 마지막 행에는 아래 시나리오를 항상 추가할 것 (식별된 시나리오 개수와 무관하게
  마지막 한 행은 항상 이 내용으로 고정)
  - 단위시험 시나리오 : SMS, 메일 발송 기능 확인
  - 예상결과 : 기능 없음
  - Path 및 T-Code : {프로그램ID}
  - 입력값 : -
  - 개발테스트결과 : 기능 없음
  - 품질테스트결과 : 기능없음
- 작성 예시 (단위시험 시나리오 / 예상결과 / Path 및 T-Code / 입력값 / 개발테스트결과 / 품질테스트결과)
  1. 주문내역조회 / 정상조회 / ZSBFMBR0580 / 하기처리내용참고 / 정상 / 정상
  2. 전표번호 더블클릭 시 FMW3 연결 / 정상조회 / ZSBFMBR0580 / 하기처리내용참고 / 정상 / 정상
  3. 예산 상세전표 단위 실적 금액 조회 시 IP 구매 정보 출력 / 정상조회 / ZSBFMBR0580 / 하기처리내용참고 / 정상 / 정상
  4. SMS, 메일 발송 기능 확인 / 기능 없음 / ZSBFMBR0580 / - / 기능 없음 / 기능 없음 (고정 마지막 행)

### 6. 사용자 테스트
- '4. 단위통합테스트결과서'에 작성한 내용을 기반으로 동일하게 작성. 다만 'SMS, 메일 발송 기능 확인' 시나리오는 제외할 것.
- C3:D3(테스트 시나리오명) : 단위통합테스트결과서 C4:E4와 동일한 시나리오 제목 작성. **병합 셀이므로 좌상단 C3에 기입. template의 이전 SR 제목이 남아있어도 반드시 이번 SR 제목으로 덮어쓸 것** (누락 빈발)
- C3:D4(업무영역) : 함수가 걸려있으므로 수정하지 않음.
- C5:C10(단위시험 시나리오) : 단위시험 시나리오를 순차적으로 작성
- E5:E10(예상결과) : 시나리오에 대한 예상 결과를 작성. 간결하게 작성할 것 (위 단위통합테스트결과서 E6:E11 간결성 규칙과 동일 — 검증 포인트 한 문장, 금액·수량 총 나열 금지)
- F5:F10(Path 및 T-Code) : {프로그램ID}
- G5:G10(입력값) : '하기처리내용참고' 입력
- H5:H10(품질테스트결과) : '정상' 입력
- 작성 예시 (단위시험 시나리오 / 예상결과 / Path 및 T-Code / 입력값 / 품질테스트결과)
  1. 주문내역조회 / 정상조회 / ZSBFMBR0580 / 하기처리내용참고 / 정상
  2. 전표번호 더블클릭 시 FMW3 연결 / 정상조회 / ZSBFMBR0580 / 하기처리내용참고 / 정상
  3. 예산 상세전표 단위 실적 금액 조회 시 IP 구매 정보 출력 / 정상조회 / ZSBFMBR0580 / 하기처리내용참고 / 정상

## 완료조건
- 파일명 규칙('NEW_ERP_{모듈명}_{SR NO}_테스트 결과서_{프로그램ID}_{영업일(토 · 일 · 공휴일 제외) 기준 오늘로부터 3일 후}.xlsx')을 준수한 산출물
  파일이 `result/{SR NO}/` 폴더에 저장됨 (AGENTS.md `## 산출물 저장 위치` 참고)
- 0. SR 정보 및 동료검토 정보, 1. 영향도 검토서가 빠짐없이 채워짐 — **특히 영향도 검토서 E38·E39·E40 세 셀 모두에 일자 기입 확인 (E39·E40 누락 빈발)**
- 4. 단위통합테스트결과서: **C4(시나리오 제목)가 이번 SR 제목으로 작성됐는지(template 이전 SR 제목 잔존 여부 확인)**, C6:C11 시나리오, E6:E11 예상결과(간결), SMS·메일 발송 기능 확인 시나리오 포함 확인
- 6. 사용자 테스트: **C3(시나리오명)이 이번 SR 제목으로 작성됐는지(template 잔존 여부 확인)**, C5:C10 시나리오, E5:E10 예상결과(간결), SMS·메일 시나리오 제외 후 나머지 반영 확인  
  — 위 조건을 실제 생성된 파일에서 확인 (AGENTS.md `정직한 보고 규칙` 준수)
